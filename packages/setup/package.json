{
  "name": "setup-cc-room",
  "version": "0.2.0",
  "description": "One-command installer for cc-room — LAN context sharing for Claude Code teams",
  "license": "MIT",
  "author": "Takanori Suzuki",
  "repository": {
    "type": "git",
    "url": "https://github.com/takanorisuzuki/cc-room.git",
    "directory": "packages/setup"
  },
  "type": "module",
  "bin": {
    "setup-cc-room": "./dist/index.js"
  },
  "files": ["dist", "vendor"],
  "scripts": {
    "build": "tsup src/index.ts src/installer.ts src/settings-merger.ts src/register-service.ts src/validators.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist vendor"
  },
  "dependencies": {
    "@cc-room/shared": "workspace:*",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsup": "^8.1.0"
  }
}
